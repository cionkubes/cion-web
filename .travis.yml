sudo: false
language: node_js
node_js:
  - node
install: yarn install
script: yarn run test
cache:
  yarn: true
deploy:
  provider: script
  script: bash scripts/deploy.sh latest
  on:
    branch: master